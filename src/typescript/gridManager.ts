// TODO continue here. Use this class to keep track of grid changes

export class GridManager {
  /* private grid: number[][];

  constructor(gridElement: Element) {}

  private processGridElement(gridElement: HTMLElement): boolean {
    let row = 0;
    let column = 0;

    for (let child of Array.from(gridElement.childNodes)) {
    }
  } */
}
